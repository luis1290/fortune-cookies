import React from "react";

const Autor = ({author}) => {
  return (
    <div>
      <div className="author">
        <span>Autor: {author}</span>
      </div>
    </div>
  );
};

export default Autor;
